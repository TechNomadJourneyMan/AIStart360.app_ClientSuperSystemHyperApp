# Slim runtime for the Vercel Python Function (read-API).
# Deliberately EXCLUDES crawler/worker/AI/graph deps (scrapy, playwright,
# crawlee, fastembed, neo4j, boto3/aiobotocore, google-genai, sentry, otel) —
# the read path (geo/competitors/analytics/regions/news/widgets/…) never imports
# them, and they would blow past Vercel's function size limit. Keep in sync with
# the routers listed in backend/app/api/v1/read_api.py::READ_ROUTERS.
fastapi>=0.115.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.30.0
pgvector>=0.3.6
pyjwt[crypto]>=2.9.0
cryptography>=43.0.0
# redis-py — small, pure-python; the read path imports it (caching/quota/deps).
# Set REDIS_URL to a reachable instance (Upstash) so those features work.
redis>=5.1.0
orjson>=3.10.0
structlog>=24.4.0
httpx>=0.27.0
tenacity>=9.0.0
python-slugify>=8.0.0
ulid-py>=1.1.0
tldextract>=5.1.0
phonenumbers>=8.13.0
rapidfuzz>=3.10.0
jsonschema>=4.23.0
feedparser>=6.0.10
python-multipart>=0.0.12
