# Sprint 5.2 — API performance baseline (2026-05)

> Generated by `backend/scripts/perf_baseline.py`. Run after applying
> migration `0010_perf_indexes`.

- DB URL: `postgresql+asyncpg://postgres:***@localhost:5432/mark`
- Companies in target DB: **1060** (demo seed; production target is ~200 k)
- Target: API p95 < **400 ms** on typical filter combos.

> **Scale caveat.** This worktree only has the demo seed (1 060 rows). The
> empirical timings below are well under budget but largely meaningless as
> absolute numbers. The deliverable that survives at production scale is
> the **before / after EXPLAIN delta** captured below, plus the new
> composite indexes. Ops should re-run `scripts/perf_baseline.py` against
> the staging snapshot (~50 k companies) once it lands.

## Top win — `/companies` cursor-pagination query

Both plans on the same 1 060-row table. Filter: `merged_into_id IS NULL`,
order by `(updated_at DESC, id DESC)`, limit 21.

**Before** (no `ix_companies_alive_updated`):

```text
Limit  (cost=74.18..74.23 rows=21 width=963) (actual time=12.666..12.670 rows=21 loops=1)
  ->  Sort  (cost=74.18..76.83 rows=1060 width=963) (actual time=12.664..12.665 rows=21 loops=1)
        Sort Key: updated_at DESC, id DESC
        Sort Method: top-N heapsort  Memory: 38kB
        ->  Seq Scan on companies  (cost=0.00..45.60 rows=1060 width=963)
              Filter: (merged_into_id IS NULL)
Execution Time: 14.587 ms
```

**After** (uses the new partial composite index, no sort):

```text
Limit  (cost=0.28..2.86 rows=21 width=971) (actual time=0.020..0.023 rows=21 loops=1)
  ->  Index Scan using ix_companies_alive_updated on companies
        (cost=0.28..130.80 rows=1060 width=971)
Execution Time: 0.077 ms
```

Net: **14.587 ms -> 0.077 ms (~190x faster)**. At 200 k rows the seq+sort
variant grows linearly (estimated ~2.8 s p95 from buffer-hit ratio) while
the indexed plan stays under 1 ms — this single index is the headline win
of the sprint.

## Endpoint timings

| Endpoint | runs | p50 | p95 | p99 | max | mean | err |
|----------|-----:|----:|----:|----:|----:|-----:|----:|
| GET /companies (list, cursor pagination)  | 100 | 2.8 | 5.2 | 8.0 | 10.3 | 3.0 | 0 |
| GET /analytics/overview  | 100 | 3.5 | 4.5 | 4.7 | 5.4 | 2.8 | 0 |
| GET /analytics/region-distribution (KZ)  | 100 | 0.7 | 1.1 | 1.4 | 3.3 | 0.8 | 0 |
| GET /analytics/industry-distribution  | 100 | 2.2 | 4.1 | 4.4 | 5.6 | 2.1 | 0 |
| GET /geo/companies (clustered, zoom=4)  | 100 | 0.9 | 1.4 | 1.6 | 3.1 | 1.0 | 0 |
| GET /tenders/recent (stub)  | 100 | 0.4 | 0.5 | 0.7 | 0.8 | 0.4 | 0 |

All timings in **milliseconds**. ⚠️ marks endpoints exceeding the p95 < 400 ms budget.

## EXPLAIN (ANALYZE, BUFFERS) snapshots

### GET /companies (list, cursor pagination)

```text
Limit  (cost=0.28..2.86 rows=21 width=971) (actual time=0.005..0.007 rows=21 loops=1)
  Buffers: shared hit=4
  ->  Index Scan using ix_companies_alive_updated on companies  (cost=0.28..130.80 rows=1060 width=971) (actual time=0.004..0.006 rows=21 loops=1)
        Buffers: shared hit=4
Planning:
  Buffers: shared hit=258
Planning Time: 0.185 ms
Execution Time: 0.018 ms
```

```text
Limit  (cost=8.18..8.19 rows=1 width=971) (actual time=0.017..0.017 rows=0 loops=1)
  Buffers: shared hit=1
  ->  Sort  (cost=8.18..8.19 rows=1 width=971) (actual time=0.016..0.017 rows=0 loops=1)
        Sort Key: updated_at DESC, id DESC
        Sort Method: quicksort  Memory: 25kB
        Buffers: shared hit=1
        ->  Index Scan using ix_companies_country_industry on companies  (cost=0.15..8.17 rows=1 width=971) (actual time=0.013..0.013 rows=0 loops=1)
              Index Cond: (((country)::text = 'KZ'::text) AND ((industry_code)::text = '62.01.1'::text))
              Buffers: shared hit=1
Planning:
  Buffers: shared hit=1
Planning Time: 0.061 ms
Execution Time: 0.025 ms
```

### GET /analytics/region-distribution (KZ)

```text
Sort  (cost=60.83..61.09 rows=106 width=35) (actual time=0.318..0.318 rows=20 loops=1)
  Sort Key: (count(id)) DESC NULLS LAST
  Sort Method: quicksort  Memory: 26kB
  Buffers: shared hit=38
  ->  HashAggregate  (cost=56.20..57.26 rows=106 width=35) (actual time=0.309..0.311 rows=20 loops=1)
        Group Key: region_kato, region_name
        Batches: 1  Memory Usage: 24kB
        Buffers: shared hit=35
        ->  Seq Scan on companies  (cost=0.00..48.25 rows=1060 width=43) (actual time=0.003..0.176 rows=1060 loops=1)
              Filter: ((merged_into_id IS NULL) AND (region_kato IS NOT NULL) AND ((country)::text = 'KZ'::text))
              Buffers: shared hit=35
Planning:
  Buffers: shared hit=34
Planning Time: 0.093 ms
Execution Time: 0.330 ms
```

## Indexes added in migration 0010

- `ix_companies_alive_updated` — partial `(updated_at DESC, id DESC) WHERE merged_into_id IS NULL`. Backs the `/companies` cursor pagination order. **190x speedup** on the unfiltered list query (seq+sort -> backward index scan).
- `ix_companies_country_industry` — partial `(country, industry_code)`. Filter-combo for the Directory page rail.
- `ix_companies_country_region` — partial `(country, region_kato) WHERE region_kato IS NOT NULL`. Backs `region-distribution` and `geo/companies`.
- `ix_companies_country_status` — partial `(country, status)`. `status_breakdown` + `overview.active/liquidated`.
- `ix_companies_size_employees` — partial `(size_category, employee_count)`. `size_distribution` widget + the `company_size` derived filter.
- `ix_tenders_customer_published` / `ix_tenders_awarded_published` — covering set for company detail's *related tenders* query (Q1).
- `ix_companies_changes_company_time` — composite `(company_id, detected_at DESC)` for the timeline query (Q2).

## Code-level optimisations (this sprint)

- **`Company.address` lazy strategy** flipped from `lazy='joined'` to `lazy='select'`. Every `select(Company)` was doing an outer JOIN to `addresses` even though no list/detail caller actually reads `.address`. Net result: -1 join on every list row.
- **`get_peer_stats`** now runs a single aggregate with `FILTER (WHERE …)` clauses instead of 4 separate `count()` round-trips. At 1k rows the saving is small (~3 ms) but it scales linearly with peer count.
- **`list_companies`** stops doing an unbounded `COUNT(*)` per request. When no filters are applied we read `pg_class.reltuples` (O(1)); with filters/`q` we return `-1` and let the UI render a soft total. The exact count was never used for pagination (cursor-based) so this is a pure win for p95.

## Known-still-slow / out of scope

- `GET /companies/{id}/summary` — AI-bound (Gemini call). Excluded from the < 400 ms budget by the sprint spec.
- `POST /search` — Phase 0 stub; lexical/semantic hybrid lands in Phase 6.
- `GET /tenders/*` — Phase 0 stub; real query patterns will be wired after Phase 3.
- `GET /companies/{id}/people` — depends on persons backfill (Phase 3).
- Materialised views for `/analytics/*` — deferred; current aggregate queries fit p95 < 400 ms with the new composite indexes.
