# Фаза 5: UX-спринт (пакеты I–IV + №30 GRI Reality Check) — Implementation Plan

> Составлен по спеке `docs/superpowers/specs/2026-07-08-ux-ideas-competitors.md` §2–§5 (решение ПО: Фаза 5 = все четыре пакета I–IV + новая идея №30). Батчи последовательны; один исполнитель за раз. Каждая фича: чистая логика под тест → API/route → UI → tsc+lint+vitest → commit.

**Goal:** Закрыть UX-проблему «получил отчёт и не понял, что делать»: превратить данные в приоритетные действия, повысить доверие к методологии, удержание и верх воронки.

## Статус (обновлено 2026-07-09, вечерний спринт)
СДЕЛАНО (реализация + тесты + авторизованный HTTP-smoke):
- ✅ **№2 Next Best Action** (54de1d0) — селектор + API + карточка на дашборде; smoke: живой ответ по реальным данным тест-аккаунта.
- ✅ **№3 «5 решающих ставок»** + **№7 бенчмарки vs топ-20%** (620cb3a) — честная формула эффекта Δ/7, ориентиры v1 с дисклеймером; в «Результате».
- ✅ **№5 методология /gri/methodology** + **№16 OG share-image /api/og/gri** (c4134e8) — включая og:image на публичной /r/[token].
- ✅ **№10 re-scan 90 дней** (in-app, окно-в-сутки без стейта) + **№11 стрик пульса** (ac38693).
- ✅ **№6 честный Trust** (5133b1f) — trustScore=null вместо заглушки 50, score по 6 измеренным блокам; Critical аудита закрыт.
- ✅ **№4 интерактивный план 90 дней** (cb66df5) — миграция 047, чек-лист, прогресс, мостик на пульс.
- ✅ **№30 GRI Reality Check** (a52f4d1) — 5 детерминированных правил БЕЗ LLM, каждое цитирует факт; smoke: нашёл реальное расхождение на тест-аккаунте.
- ✅ **№13 sample + №14 якорь vs консалтинг + №18 валюта по гео** — на лендинге (7d10eeb, вместе с 6E).

БЭКЛОГ (сознательно НЕ в этом спринте):
- **№1 AI-чат по отчёту + RAG** — требует выделенного батча с анти-галлюцинационным ревью (embed.ts пишет vector(1536), retrieval не построен). Не пихать в хвост спринта.
- **№8 источники Mark-analytics** — зависит от интеграции внешнего приложения /market.
- **№12 «что разблокировалось»** (тосты) и **№17 отраслевые mini-GRI** — мелкие, следующая итерация.
- №10-часть «милстоун-экран празднования роста» — re-scan есть, празднование не делалось.

## Батч 5A — Пакет I «Понимание → действие» (оставшиеся)
### №5 «5 решающих ставок» (S · 🔥🔥)
- Чистый трансформер `lib/gri/decisive-bets.ts`: `top_5_limits` → `{ ставка, ожидаемый эффект на GRI, первый шаг }`. Первый шаг брать из `action_plan_90d`/quick_wins, эффект — из severity/score блока.
- Компонент `components/gri/page/DecisiveBets.tsx` во вкладке «Результат» GRI. Тест трансформера.
### №4 Интерактивный план 90 дней (M · 🔥🔥🔥)
- Миграция 047: `gri_plan_progress (user_id, assessment_id, step_key, done_at)` RLS own.
- `action_plan_90d` → чек-лист с галочками, % выполнения; API `/api/v1/gri/plan-progress` GET/PATCH (upsert done). Связь с пульсом: «выполнили 3 шага — пересними пульс» (тост/подсказка Гри).
### №1 AI-чат по отчёту (M · 🔥🔥🔥)
- Чат Гри уже есть; прокинуть контекст текущего отчёта (Point A/GRI снимок) в prompt. RAG: `lib/documents/embed.ts` (vector(1536) заполняется, но не читается) — добавить retrieval top-k чанков по эмбеддингу вопроса в контекст. Осторожно: отдельный батч, ревью анти-галлюцинаций.

## Батч 5B — Пакет II «Доверие и методология»
### №6 Честный Trust-блок (S · 🔥🔥, Critical аудита 2026-07-07)
- ⚠ РИСК: `lib/gri/logic.ts:101 trustScore = 50` — ядро скоринга; в проекте несколько GRI-движков (см. [[aistart360-module-audit]]). Сначала разведка: какой движок ЖИВОЙ (пишет gri_assessments), затем считать trust из реальных сигналов анкеты (s9n_*), либо честно «нет данных». Тесты на формулу. НЕ менять вслепую — сдвинет всем score.
### №5(методология) «Как считается GRI» (S-M · 🔥🔥)
- Статическая страница `/gri/methodology`: 7 блоков, критерии, шкала, честные ограничения; ссылка из «Результата» и в PDF.
### №7 Бенчмарки «вы vs топ-20%» (M · 🔥🔥)
- Хардкод-медианы 5–7 отраслей СНГ (`lib/gri/benchmarks.ts`), gap-chart в «Результат» + PDF. Потом — живая анонимная база.
### №8 Источники под рыночными выводами (M · 🔥🔥)
- В маркет-анализе: «по данным N компаний/тендеров Mark-analytics, дата» с раскрытием.

## Батч 5C — Пакет III «Retention»
### №10 Re-scan через 90 дней + милстоуны (S-M · 🔥🔥)
- Напоминание «пора пересчитать GRI» (in-app/дайджест Фазы 4); при росте — экран «GRI 3.2 → 5.7» + радующийся Гри.
### №11 Стрик пульса (S · 🔥) — серия недель подряд → огонёк/бейдж у Гри (из gri_pulse_responses).
### №12 «Что разблокировалось» (S · 🔥) — тост после важных сохранений (анкета/GRI).

## Батч 5D — Пакет IV «Верх воронки»
### №16 Share-картинка GRI (S-M · 🔥🔥) — динамический OG-image (`app/api/og/gri/[token]`) с радаром+score.
### №13 Sample-отчёт на лендинге (S · 🔥🔥) — обезличенный GRI-PDF + демо.
### №14 Якорь vs консалтинг + ROI-калькулятор (S · 🔥🔥) — блок на лендинге.
### №17 Отраслевые mini-GRI в /gri-free; №18 локализация валюты по IP.
> №15 (авто-одобрение self-serve) переезжает в Фазу 6 (Пакет VIII).

## Батч 5E — №30 GRI Reality Check (M · 🔥🔥🔥, новая идея ПО)
- AI-сверка самооценки GRI-теста с фактами анкеты (144 вопроса) + метрик: расхождения вида «Финансы оценены на 8, но runway 2 мес». Опора на Smart Assistant (validation/inconsistency уже есть) + OpenRouter.
- `lib/gri/reality-check.ts`: детерминированные правила расхождений (block-score vs фактические поля анкеты) → список несоответствий; LLM-слой только формулирует. Кэш в gri_assessments (jsonb `reality_check`).
- Вывод в «Результат» GRI + в отчёт.

## Не делаем в Фазе 5
Пакеты V–VII (бэклог), валидация идей, генерация бизнес-планов, полный conversational-онбординг.
